export { SupaProxyClient, SupaProxyError } from './client';
export type { ClientOptions, RequestOptions } from './client';

// Re-export shared types for convenience
export type * from '@supaproxy/shared';
