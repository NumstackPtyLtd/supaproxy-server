// No seed data. Everything is created through the setup flow.
// This file is kept as a placeholder in case we need dev fixtures later.

export async function seed() {
  // Intentionally empty. The /setup flow creates the organisation,
  // admin user, and initial settings. Workspaces are created through
  // the UI after setup.
}
